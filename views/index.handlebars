<h1>Burger</h1>

<h2>Order a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="nBurger">Devour it?</label><br>
    {{!-- <input type="radio" name="nBurger" value="1" checked> Sleepy!<br>
    <input type="radio" name="nBurger" value="0"> Awake --}}
  </div>

  <button type="submit">Add Burger</button>
</form>



<h2>Burger List</h2>

<ul>
  {{#each burgers}}
    {{#unless nburger}}
      {{> burgers/burger-block burger=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers Eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if nburger}}
      {{> burgers/burger-block burger=false}}
    {{/if}}
  {{/each}}
</ul>


